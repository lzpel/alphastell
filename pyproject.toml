[project]
name = "examples"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = ["alphastell", "openmc-anywhere>=0.15.3.0", "cad-to-dagmc", "matplotlib", "typst"]

[tool.uv.sources]
alphastell = { path = "./alphastell" }
